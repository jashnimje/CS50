SELECT name FROM people WHERE name <> "Kevin Bacon" AND id IN (SELECT DISTINCT(person_id) FROM stars WHERE movie_id IN (SELECT movies.id FROM movies JOIN stars ON movies.id = stars.movie_id JOIN ratings ON ratings.movie_id = movies.id JOIN people ON people.id = stars.person_id WHERE people.name = "Kevin Bacon" AND birth = "1958"));